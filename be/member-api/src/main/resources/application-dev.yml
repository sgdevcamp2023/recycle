server:
  port: 8081
spring:
  config:
    use-legacy-processing: true
  profiles:
    include:
      - mysql-dev
      - flyway-member
      - security-dev
      - cookie-dev
  redis:
    host: ${REDIS_HOST}
    port: ${REDIS_PORT}
    password: ${REDIS_PASSWORD}
  session:
    store-type: redis
    redis:
      namespace: zzuag:session